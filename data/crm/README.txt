Local CRM + market intel SQLite file: ops.sqlite3 (created by scripts/crm_cli.py init).

This directory is safe to back up. The database is gitignored once populated — copy it to your backup drive or sync tool.

Tests and scripts/run_ops_demo.py may set OPS_SQLITE_PATH to use a temporary database instead of this path.
