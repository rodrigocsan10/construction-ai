Optional company logo for branded proposals
===========================================

Place a PNG or JPG here, e.g. `logo.png`, then set in proposal JSON:

  "company": { "logo_path": "assets/logo.png", ... }

`build_proposal_pdf.py` resolves paths relative to the repository root.
